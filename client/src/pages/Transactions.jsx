import { Layout } from "../components";
import SideBar from "../components/SideBar";

const Transactions = () => {
  return (
  <Layout>
    <p>Transactions</p>
  </Layout>
  );
};

export default Transactions;
